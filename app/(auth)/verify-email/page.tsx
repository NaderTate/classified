import { VerificationForm } from "./_components/VerificationForm";

type verifyEmailPageProps = {};

const verifyEmailPage = async ({}: verifyEmailPageProps) => {
  return <VerificationForm />;
};

export default verifyEmailPage;
