{
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "cd ../.. && bun run --filter=@classified/shared build && cd apps/web && bun run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
